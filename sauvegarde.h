/*int main(void);
{
    FILE *fic = fopen("sauvegarde.c", "r+");
    int iteration = 0;
    
    if (fic == NULL)
      exit(1);
}

int fic;

for (fic = 0 ; fic <100000 ; fic++)
{
  scanf(fic, %d, &iteration);
  printf("Sauvegarde : %d\n", iteration);
}

fclose(fic);

return 0;
}*/
